{
  "name": "site-nineyards",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "deploy": "bash deploy.sh",
    "offline": "bash offline.sh",
    "preview": "bash build.sh && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
